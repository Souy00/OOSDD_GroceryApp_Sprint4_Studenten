name: MAUI CI - Android Only

on:
  push:
    branches:
      - main
      - infra/runner-toevoegen
  pull_request:
    branches:
      - main

jobs:
  android-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      # Alleen Android workload installeren
      - name: Install MAUI Android workload
        run: dotnet workload install maui-android

      # Restore voor alle projecten, iOS wordt genegeerd
      - name: Restore all projects
        run: dotnet restore /p:TargetFramework=net8.0-android

      # Build class libraries zonder Android target (gebruik eigen TargetFramework)
      - name: Build class libraries
        run: |
          dotnet build Grocery.Core/Grocery.Core.csproj --configuration Release
          dotnet build Grocery.Core.Data/Grocery.Core.Data.csproj --configuration Release

      # Build Android-app met Android target
      - name: Build Android app
        run: dotnet build Grocery.App/Grocery.App.csproj -f net8.0-android --configuration Release

      # Unit tests uitvoeren
      - name: Run unit tests
        run: dotnet test --configuration Release --verbosity normal
